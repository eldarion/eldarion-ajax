version: 2

jobs:
  build:
    docker:
      - image: circleci/node:7.10
    working_directory: ~/repo
    steps:
      - checkout
      - restore_cache:
          keys:
            - v3-dependencies-{{ checksum "package.json" }}
      - run: yarn install
      - run: yarn add codecov
      - save_cache:
          paths:
            - node_modules
          key: v3-dependencies-{{ checksum "package.json" }}
      - run: yarn test
      - run:
          name: upload coverage reports
          command: ./node_modules/.bin/codecov --file tests/coverage/report.json
